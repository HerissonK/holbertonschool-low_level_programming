#include <stdio.h>

/**
 * main - print the alphabet in lowercase, followed by a new line.
 *
 * Return: Always 0
 */

int main(void)
{
	int num;

	for (num = 0; num < 15; num++)
		putchar("%x" + num);

	putchar('\n');
	return (0);
}
